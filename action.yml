name: linter
description: |
  A GitHub Action for linting (eg. Bash, Go, CloudFormation, Dockerfiles, etc)
author: jcleal

runs:
  using: composite
  steps:
    - shell: bash
      run: entrypoint.sh

branding:
  icon: user-check
  color: yellow
